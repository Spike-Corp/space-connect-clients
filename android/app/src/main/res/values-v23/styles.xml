<resources>

    <!--
        Base application theme for API 23+. Identical to values-v21/styles.xml but ALSO sets
        android:windowLightStatusBar (needs API23+) so the status bar icons flip to dark-on-light
        when the Light theme is active - safe here since this bucket only ever matches API23+
        devices (unlike values-v21, which also covers API21-22 where this attribute doesn't
        exist and would corrupt style resolution).
    -->
    <style name="AppBaseTheme" parent="android:Theme.Material">
        <item name="appBackgroundColor">@color/moonlight_background</item>
        <item name="appSurfaceColor">@color/moonlight_surface</item>
        <item name="appSurface2Color">@color/moonlight_surface_2</item>
        <item name="appForegroundColor">@color/moonlight_foreground</item>
        <item name="appMutedForegroundColor">@color/moonlight_muted_foreground</item>
        <item name="appBorderColor">@color/moonlight_border</item>
        <item name="appLightSystemBars">false</item>
        <item name="appBackgroundDrawable">@drawable/bg_app_gradient</item>
        <item name="android:statusBarColor">?attr/appBackgroundColor</item>
        <item name="android:navigationBarColor">?attr/appBackgroundColor</item>
        <item name="android:windowLightStatusBar">?attr/appLightSystemBars</item>
        <item name="android:windowBackground">?attr/appBackgroundDrawable</item>
        <item name="android:colorBackground">?attr/appBackgroundColor</item>
        <item name="android:colorPrimary">?attr/appSurfaceColor</item>
        <item name="android:colorPrimaryDark">?attr/appBackgroundColor</item>
        <item name="android:colorAccent">@color/moonlight_purple</item>
        <item name="android:textColorPrimary">?attr/appForegroundColor</item>
        <item name="android:textColorSecondary">?attr/appMutedForegroundColor</item>
        <!-- Dialogs (spinner de conexão do Game, 2FA, confirmações) sempre no tema
             escuro da marca. Sem isso, OEMs que clareiam dialogs do sistema (OneUI/
             Samsung é o caso conhecido) desenham o AlertDialog com fundo claro do
             sistema + nosso texto claro = mensagem invisível ("não mostra o que está
             acontecendo na máquina"). -->
        <item name="android:alertDialogTheme">@style/SpaceAlertDialog</item>
    </style>

    <style name="SpaceAlertDialog" parent="android:Theme.Material.Dialog.Alert">
        <!-- Fundo escuro fixo → texto precisa ser o claro da paleta dark SEMPRE
             (não ?attr/appForegroundColor, que no tema Light vira escuro e sumiria). -->
        <item name="android:background">@color/moonlight_surface</item>
        <item name="android:colorBackground">@color/moonlight_surface</item>
        <item name="android:textColorPrimary">@color/moonlight_foreground</item>
        <item name="android:textColorSecondary">@color/moonlight_muted_foreground</item>
        <item name="android:colorAccent">@color/moonlight_purple</item>
    </style>

</resources>
